// Write a TypeScript function processValue that takes string | number | boolean | null | undefined, uses type 
// guards to log uppercase strings, numbers fixed to two decimals, and boolean as "True"/"False,” and throws 
// an error for null or undefined, including a type assertion to narrow the type.

